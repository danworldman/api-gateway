apiVersion: apps/v1
kind: Deployment
metadata:
  name: system-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: system-redis
  template:
    metadata:
      labels:
        app: system-redis
    spec:
      containers:
      - name: redis
        image: redis:7-alpine
        ports:
        - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: system-redis
spec:
  selector:
    app: system-redis
  ports:
  - port: 6379
    targetPort: 6379
  type: ClusterIP
